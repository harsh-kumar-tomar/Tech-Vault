```
# list all images
docker images 

# to pull images
docker pull name

# The docker build command creates a Docker image from a Dockerfile placed at the provided path.

docker build -t myapp:latest
```